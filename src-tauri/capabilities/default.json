{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the BiliCast main window",
  "windows": ["main", "danmaku-overlay", "event-sidebar"],
  "permissions": ["core:default", "dialog:allow-open"]
}
