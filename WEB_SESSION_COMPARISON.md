# BiliCast 匿名 Web 与登录态 Web 数据对照

> 记录日期：2026-08-05
> 当前状态：匿名态与登录态进场展示已完成同房间人工对照

## 1. 结论

1. **BiliCast 的默认运行链路可以独立于旧弹幕姬。** 对于“本机接收直播事件 → 规则过滤 → 语音播报”的产品目标，扫码登录 Web 长链已经覆盖核心入口，不需要另一个弹幕工具充当中转。
2. **登录态实测确实改善了进场身份信息。** 匿名态样本中的进场昵称是带星号的脱敏字符串，头像为空或为默认头像；登录态截图中，同一事件流已经显示完整昵称与对应头像。
3. **完整昵称/头像与稳定用户标识要分开记录。** 当前截图直接证明的是昵称和头像。BiliCast 现已保留 `INTERACT_WORD_V2` 第 1 字段以及弹幕、礼物、SC、上舰 JSON 中的数字 UID；服务端实际下发非零 UID 时，事件行会显示 `UID ...`。
4. **扫码登录不会把观看端变成主播后台。** 它改变的是 Web 观看会话身份与服务端返回字段，不附带主播授权、房管、开播控制或官方互动玩法会话。
5. **官方身份码不是旧时代遗留。** 它属于 OpenLive 的应用授权模型，用来绑定主播、开发者项目与一场可管理的官方会话；与 Web 观看账号扫码解决的是两类问题。

## 2. 本次人工验证

验证房间：`4457340`

| 检查项 | 匿名 Web | 登录态 Web | 当前结论 |
|---|---|---|---|
| 长链鉴权观看 UID | `0` | 扫码账号真实 UID | 已验证连接模式切换 |
| 进场昵称 | 星号脱敏昵称 | 完整昵称 | 已由截图验证 |
| 进场头像 | 空值或默认头像较常见 | 多数为对应用户头像 | 已由截图验证，仍保留加载失败兜底 |
| 进场用户数字 UID | 匿名 V2 样本中缺失或为 `0` | 协议具有 UID 字段 | 已完成解析与 UI 展示，等待非零真实包留样 |
| 房间切换 | 支持公开房间号 | 支持公开房间号 | 两种模式一致 |
| 礼物、SC、上舰 | 可接收 | 可接收，身份字段通常更完整 | 已有统一事件解析 |
| 关注、分享等互动 | 事件类型存在，身份可能脱敏 | 事件类型存在，身份展示更完整 | 需要用测试账号触发留样 |
| 主播后台权限 | 无 | 无 | 两种模式一致 |

### 证据边界

- 截图中的完整昵称和头像是本次最直接的 A/B 证据。
- 截图没有展示进场用户的数字 UID，因此 UID 仍以事件行后续出现的 `UID ...` 或原始诊断样本为最终证据。
- Web 长链字段由站内服务控制；登录态显著改善了本次样本，但产品逻辑仍应接受字段为空、默认头像和昵称再次脱敏的情况。

## 3. Web 互动事件

当前 Web 长链的 `INTERACT_WORD_V2` 使用 Base64 Protobuf。BiliCast 已覆盖以下 `msg_type`：

| `msg_type` | BiliCast 展示 | 说明 |
|---:|---|---|
| `1` | 进场 | 用户进入直播间 |
| `2` | 关注 | 用户关注主播 |
| `3` | 分享 | 用户分享直播间 |
| `4` | 特别关注 | 用户将主播设为特别关注 |
| `5` | 互粉 | 用户与主播形成互相关注 |
| `6` | 点赞 | 用户为主播点赞 |

因此，**登录态 Web 可以收到关注类互动**；是否在某次操作中出现，还取决于服务端是否推送该命令：

- 测试账号原本已经关注主播时，再点击关注不会形成新的关注状态变化；
- 建议先用测试账号取消关注，再在直播间开播期间重新关注；
- 点赞通常高频发生，平台可能聚合或限频，播报层应设置合并窗口，避免连续朗读；
- 匿名和登录态都可能收到互动命令，登录态的主要优势是本次实测中的身份字段完整度。

作为实现对照，`blivedm` 当前 Web 模型也把 `INTERACT_WORD_V2` 定义为进入/关注等互动，并列出了 `1` 到 `6` 的相同含义：<https://github.com/xfgryujk/blivedm/blob/dev/blivedm/models/web.py>。

## 4. 为什么旧弹幕姬使用主播身份码

官方 OpenLive 示例要求：

- `AccessKeyId`
- `AccessKeySecret`
- `AppId`
- 主播身份码 `Code`

随后执行：

1. `StartInteractivePlay(code, appId)` 启动项目会话；
2. 获取 `game_id`、官方 WSS 地址和鉴权体；
3. 维持项目心跳；
4. 接收官方标准化事件；
5. `EndInteractivePlay(appId, gameId)` 结束会话。

官方示例：<https://github.com/bilibili-openplatform/OpenLive_CSharpDemo/blob/main/OpenBLiveSample/Program.cs>

这种设计提供了 Web 观看长链以外的属性：

| 属性 | 登录态 Web | OpenLive 身份码 |
|---|---|---|
| 授权主体 | 扫码的观看账号 | 主播授权某个开发者项目 |
| 房间范围 | 可切换公开房间 | 身份码对应的主播/项目会话 |
| 用户标识 | 站内 Web UID，字段受会话和隐私策略影响 | 文档化的 `open_id`，可申请同开发者 `union_id` |
| 数据结构 | 站内 Web 命令，可能变化 | 官方公开 CMD 与字段定义 |
| 会话管理 | Cookie、令牌、WSS 心跳 | START、项目心跳、END |
| 中心服务 | 本地个人应用可全程本机运行 | 公开发行时通常由服务端保护项目密钥并签名 |

所以旧弹幕姬选择身份码，更多是**官方接入路线、产品定位和密钥保护策略**，而不是当时技术做不到扫码登录。

## 5. 为什么旧弹幕姬可能没有“关注”事件

这更像是版本和事件映射差异：

- 官方 C# 示例当前注册了弹幕、礼物、上舰、SC、点赞和进场回调，但示例代码里没有注册关注回调；
- 当前 OpenLive CMD 文档已经定义用户关注通知 `OPEN_LIVEROOM_INTERACT_WORD`；
- 当前文档也定义点赞事件 `OPEN_LIVEROOM_LIKE`，并说明仅开播时触发、按单用户最近 2 秒聚合点赞次数。

官方 CMD 文档：<https://bilibili.apifox.cn/doc-7499813>

因此，某个弹幕姬界面没有关注 Event，可能是它使用的 SDK/协议版本较早、没有把新 CMD 暴露成 UI 事件，或产品层主动忽略了关注通知。

## 6. BiliCast 架构决策

建议采用：

```text
LiveAdapter
├── WebLiveAdapter
│   ├── AnonymousSession
│   └── AuthenticatedSession   # 当前默认推荐
└── OpenLiveAdapter            # 保留接口位，按正式发行需求接入
        ↓
EventNormalizer
        ↓
RuleEngine → SpeechQueue → TtsAdapter
```

具体决策：

1. 从日常运行流程中移除对旧弹幕姬的依赖；
2. 扫码登录 Web 作为 BiliCast 默认模式，匿名 Web 作为降级与对照模式；
3. 保留 `LiveAdapter` 抽象，而不是把 Web 命令直接耦合到播报规则；
4. OpenLive 作为可选官方适配器，面向需要主播项目授权、稳定 `open_id` 或官方服务边界的场景；
5. 本地版继续让 Cookie 只存在 Rust 进程内，不建设中心化账号服务器。

## 7. 关注与点赞验证步骤

1. 使用账号 A 扫码登录 BiliCast，并连接正在直播的目标房间；
2. 使用账号 B 确认尚未关注该主播；
3. 账号 B 在直播间执行关注，观察“互动”筛选中是否出现“关注”；
4. 账号 B 连续点赞数次，观察是否出现“点赞”；
5. 检查昵称、头像和事件行中的 `UID ...`；
6. 记录 `rawCommand`、时间和会话模式，作为协议回归样本；
7. 退出 BiliCast 登录态后重连同一房间，重复一次以确认匿名态字段差异。
